name: Delete old artifacts
on: workflow_dispatch
jobs:
  delete-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: geekyeggo/delete-artifact@v5
        with:
          name: |
            *
          useGlob: true
          failOnError: false
